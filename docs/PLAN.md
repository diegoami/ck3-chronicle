# ck3-chronicle — the plan

Decided on diegoami/Ck-parser#46 (the proposal, its review and the owner's
decisions). This file is the product's plan; the proof of concept's
`docs/PLAN.md` holds the verified facts about the save format, which this
does not repeat.

## 1. What it is

A directory of Crusader Kings III save games in, a static wiki out: one
**chronicle** per playthrough, with pages for titles, characters, houses,
cultures and faiths, realms, portraits and coats of arms. Three ways to run it:

| edition | who | how |
|---|---|---|
| library + CLI | anyone with Python | `pip install ck3-chronicle`, then `ck3chronicle build SAVES OUT` |
| GitHub edition | anyone who wants it published | `ck3chronicle init github DIR` + `setup`: a repository whose workflow builds the wiki from saves uploaded to its Releases |
| desktop edition | players on Windows | a Tk window: pick the save folder and an output folder, build, open |

## 2. Decisions (M0, owner, 2026-09-24)

| decision | chosen |
|---|---|
| layout | this repository: library + CLI + Tk GUI; a **template repository** for the GitHub edition, generated by the library's own `init` |
| name | `ck3-chronicle` on PyPI, `ck3chronicle` to import |
| license | MIT |
| GUI | Tk / ttk (standard library) |
| desktop platforms | Windows first: an unsigned `.exe` built by CI per release (SmartScreen will warn). macOS/Linux later; `pip install` works everywhere |
| Neo4j | a **separate add-on project, later**, depending on the library; this package never imports it |
| GitHub edition visibility | `init` offers **public** (Pages; saves and site public; the README says so) and **private** (no Pages; the site built into a downloadable archive). **Public by default.** GitHub Free serves Pages only from public repositories, and even on Pro a private repository's Pages site is public |
| the POC | stays active for experiments; its product issues move here as they are ported |

## 3. Layout

```
src/ck3chronicle/
  core/      container, parser, fingerprint, runs, titles, family, digest,
             dynasties, arms, cultures, faiths, characters, player, filter,
             realm, vassalage, naming (the image-name rule), snapshot
             (SnapshotView, gather, lineage, resolve_saves), history
             (holder_intervals), population (the full character stream)
  wiki/      model, render, manifest, queue, prose, maps
  api.py     build(saves, out, config, progress) -> Result
  config.py  ck3chronicle.toml
  cli.py     ck3chronicle build | init | setup | gui | maps | queue
  gui/       Tk (M6)
  scaffold/  the GitHub edition's templates (M5)
```

A sketch, not a contract: the milestone that builds a part may refine it, and
says so on its issue. The dependency direction is not negotiable: `core` imports
nothing else in the package, `wiki` imports `core`, the editions import both,
and nothing imports an edition.

## 4. Porting map (from Ck-parser)

| POC | here | notes |
|---|---|---|
| `ck3parser/{container,parser,fingerprint,runs,titles,family,digest,dynasties,arms,cultures,faiths,characters,player,filter,realm,vassalage}.py` | `core/` | as they are, tests with them |
| `ck3parser/portraits.py` | `core/naming.py` | the image-name rule, unchanged: a contract with the companion |
| `ck3parser/pipeline.py`: `SnapshotView`, `collect_characters`, `lineage`, `gather`, `resolve_saves` | `core/snapshot.py` | cut loose from the graph: the POC module imports `ck3graph` |
| `ck3graph/loader.py`: `holder_intervals` | `core/history.py` | the wiki's tenures come from it |
| `ck3graph/people.py`: `stream_people` | `core/population.py` | a public API, so the graph add-on needs nothing private (Ck-parser#48) |
| `ck3wiki/{model,render,manifest,queue,prose,maps}.py` | `wiki/` | hard-wired values become configuration (§6) |
| `ck3wiki/build.py` | `api.py` + `cli.py` | `build()` reports progress through a callback; logging, not `print` |
| `tests/`, `tests/fixtures/gamestate_sample.txt`, `tests/helpers.py` | `tests/` | the fixture is the POC's; keep it byte for byte |
| `ck3parser/sections.py` | `ck3chronicle debug sections` | diagnostic, not on the build path |
| `ck3parser/consistency.py` | later | only the POC's graph pipeline uses it; worth porting as a run check |
| `ck3parser/handoff.py` | **not ported** | retired: `portraits.json` is the one queue (Ck-parser#27) |
| `ck3graph/*`, the rest of `pipeline.py` | **not ported** | the graph add-on, later |

## 5. Parity

Porting must not change a single page. The reference is the POC's own output:

1. The POC session tags a Ck-parser commit `poc-reference-1` and attaches
   `site-sha256.txt` to a release of that tag: one line per generated file,
   `sha256  path`, for a build of the five release saves.
2. Here, `scripts/parity.sh` builds the same five saves with
   `scripts/parity.toml`, which configures the POC's own links (the page
   footer, the landing page's companion link, `portraits.json`'s `docs`
   block), and checks every file with `sha256sum -c`. **Byte-identical, with
   no normalisation**: the links differ from the POC's only when configured to.
3. A later reference (`poc-reference-2`, …) replaces the tag in
   `PARITY_REFERENCE`, and `docs/HANDOVER.md` names the one in force.

The saves: `SAVES_REPO=diegoami/ck_wiki` with the POC's `scripts/fetch_saves.sh`
(~330 MB, into a git-ignored `saves/`).

## 6. Configuration

`ck3chronicle.toml`, every key optional, an unknown key an error (built in M3;
the full shape is in `src/ck3chronicle/config.py`'s docstring):

- `[site]`: the footer's generator name and URL, the companion's name and link
  (none by default), and `[site.docs]`, the `portraits.json` `docs` links (by
  default the product's own `docs/IMAGES.md`, and no `images` link);
- `[saves] repository`: whose Releases `ck3chronicle fetch` reads;
- `[runs]`: a subject title for every run, and `[runs.titles]` per run (else
  the played character's primary title);
- `[reach]`: vassals, family, kin, siblings, titled kin (the POC's `--no-*` flags);
- `[images] dir`: where delivered portraits, arms and maps are read from;
- `[cache] dir`: the per-save character digests;
- `[prose]`: its directory, backend, URL, model and user agent; the key only
  ever from the environment (`CK3_PROSE_API_KEY`), as is a GitHub token;
- maps (M7): the game directory, else found where Steam installs it. **Game
  files are used only for saves of the same game version** (Ck-parser#58,
  owner's decision: strict, no override): the install's `rawVersion` in
  `launcher/launcher-settings.json` must equal the run's `version`, or that
  run gets no map and no game text.

## 7. Milestones

One issue each; one PR or a few each, under the review rule.

| # | milestone | size | done when |
|---|---|---|---|
| M1 | skeleton | S | src layout, `pyproject.toml` (extras `maps`, dev group), CI on Linux + Windows, PyPI trusted publishing configured, `CLAUDE.md`, the review skill, a SessionStart hook for web sessions |
| M2 | core ported, free of Neo4j | M | the POC's parser-side tests and fixture ported and green; `neo4j` imported nowhere; `core` imports nothing else in the package |
| M3 | wiki + API + config | M | `build()` with progress callbacks; nothing hard-wired; **parity** (§5) on the five saves |
| M4 | 0.1.0 on PyPI; ck_wiki runs on it | S | ck_wiki's workflow installs `ck3-chronicle==0.1.0` and the published wiki is unchanged (owner's go needed: it writes to ck_wiki) |
| M5 | GitHub edition | M | `init github` (public or private) + `setup` (create the repository, **Pages set to the Actions source**, upload saves, first dispatch); a template repository; a fresh repository publishes from one uploaded save with no manual step but a token |
| M6 | desktop edition | M–L | on a clean Windows machine the `.exe` opens, finds the save folder, builds with progress into a chosen folder, opens the result; a folder with no saves is a friendly error |
| M7 | game-file features | M | realm maps ported from the POC (Ck-parser#50); localization (Ck-parser#31) decided and, if chosen, built — both where the game is: the desktop and the CLI |

## 8. What the POC learnt that the editions must keep

- **GitHub Pages must use the GitHub Actions source**, not a branch (ck_wiki
  lost several runs to it).
- **Uploading an asset into an existing release does not dependably fire a
  `release` event.** The dependable trigger is `repository_dispatch`; a daily
  schedule is the backstop.
- **A build reads ~1.1 GB at peak** and takes ~4½ minutes cold, ~1½ warm, for five
  saves in three runs; the per-save cache is 43 MB, the site 125 MB. The
  desktop builds one chronicle at a time and shows progress.
- **Game data drifts between patches** (provinces renumbered, baronies added,
  text rewritten), so output drawn from another version's files can look right
  and be wrong. A player's install is usually the latest patch, so older
  playthroughs get no maps or game text: the desktop says which chronicles its
  game can serve. Unverified, and to settle before M7: whether a save's
  `version` is the version that wrote it or the one its run started on
  (Ck-parser PLAN.md §5).
- **The owner's review loop catches real defects** (a wrong figure, an
  order-dependent rank, a Windows-only crash, a vanished save): budget for it.
